/**
 * DTO déplacé vers src/common/dto/pagination-query.dto.ts à l'étape 4
 * (mutualisation users / admin / posts / feed) — ré-exporté ici pour ne pas
 * casser les imports existants du module users et du module admin.
 */
export { PaginationQueryDto } from '../../../common/dto/pagination-query.dto';
